import * as React from "react";
import { render } from "react-dom";
 
const App = () => {
  return (
    <div>
      <p>React / TypeScript / Parcel starter</p>
    </div>
  );
};
 
render(<App />, document.getElementById("app"));